<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
    　 <%= render 'books/user', user: @user %>
     
      <h2>New book</h2>
       <%= render 'books/new', book: @book %>
     
     <div class="col-md-8 offset-md-1">
       <h2>Users</h2>
       <table class="table">
         <thead>
           <tr>
             <th>image</th>
             <th>name</th>
             <th></th>
             <th colspan="3"></th>
           </tr>
         </thead>
         <tbody>
           <% @books.each do |book| %>
           <tr>
             <td>
              <%= link_to book.user do %>
              <%= image_tag book.user.get_profile_image(50,50) %>
              <% end %>
             </tb>
             <tb><%= link_to book.title %></tb>
             <tb><%= book.body %></tb>
           <% end %>
           </tr>
         </tbody>
         </table>
      </div>
   </div>
</div>